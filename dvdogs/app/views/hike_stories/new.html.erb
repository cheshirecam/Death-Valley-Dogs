<div class="showTitleObject">New Write-up</div>
<% @hike_story.hike_id = params[:hike_id] %>
<% form_for(@hike_story,:html => { :multipart => true }) do |f| %>

  <%= f.error_messages %>
  <p>
    <%= f.label :dog_id, "According to" %><br/>
    <%= f.collection_select(:dog_id, Dog.all, :id, :name) %>
  </p>
  <p>
    <%= f.label :hike_id, "Hike" %><br/>
    <%= f.collection_select(:hike_id, Hike.all, :id, :name) %>
  </p>
  <p>
    <%= f.label 'Photo' %><br />
    <%= f.file_field :photo %>
  </p>

  <%= render :partial => "/shared/writeup_synopsis_and_editor", 
            :locals => {
            :f => f } %>

  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', hike_stories_path %>